# พจนานุกรมข้อมูลและโครงสร้างฐานข้อมูล (Data Dictionary & Database Schema)
## ระบบจัดการเอกสารดิจิทัลพร้อมการลงนามอิเล็กทรอนิกส์อัจฉริยะ (DocMS)
**ฐานข้อมูล**: `docms_db` | **Charset**: `utf8mb4` | **Collation**: `utf8mb4_unicode_ci`

---

## สารบัญตาราง (Database Tables Summary)
1. [ตาราง `users` - ข้อมูลผู้ใช้งานและสิทธิ์](#1-ตาราง-users---ข้อมูลผู้ใช้งานและสิทธิ์)
2. [ตาราง `documents` - ข้อมูลเอกสารและการลงนาม](#2-ตาราง-documents---ข้อมูลเอกสารและการลงนาม)
3. [ตาราง `signatures` - ลายมือชื่ออิเล็กทรอนิกส์ส่วนบุคคล](#3-ตาราง-signatures---ลายมือชื่ออิเล็กทรอนิกส์ส่วนบุคคล)
4. [ตาราง `certificates` - ใบรับรองดิจิทัล (CA)](#4-ตาราง-certificates---ใบรับรองดิจิทัล-ca)
5. [ตาราง `notifications` - ระบบการแจ้งเตือน](#5-ตาราง-notifications---ระบบการแจ้งเตือน)
6. [ตาราง `access_requests` - คำร้องขอสิทธิ์เข้าถึงเอกสาร](#6-ตาราง-access_requests---คำร้องขอสิทธิ์เข้าถึงเอกสาร)
7. [ตาราง `document_access` - สิทธิ์การเข้าถึงเอกสารเฉพาะบุคคล](#7-ตาราง-document_access---สิทธิ์การเข้าถึงเอกสารเฉพาะบุคคล)
8. [ตาราง `activity_logs` - บันทึกประวัติการใช้งาน (Audit Logs)](#8-ตาราง-activity_logs---บันทึกประวัติการใช้งาน-audit-logs)

---

### 1. ตาราง `users` - ข้อมูลผู้ใช้งานและสิทธิ์
จัดเก็บข้อมูลบัญชีผู้ใช้งาน, รหัสผ่าน, บทบาท, แผนก, และความปลอดภัยในการเข้าสู่ระบบ

| ชื่อคอลัมน์ (Column) | ชนิดข้อมูล (Data Type) | คำอธิบาย (Description) | ตัวอย่างข้อมูล |
| :--- | :--- | :--- | :--- |
| `id` | `VARCHAR(50)` [PK] | รหัสประจำตัวผู้ใช้ (Primary Key) | `u001`, `u1771926013618` |
| `username` | `VARCHAR(100)` [Unique] | ชื่อผู้ใช้สำหรับใช้เข้าสู่ระบบ (ห้ามซ้ำ) | `admin`, `somchai` |
| `password` | `VARCHAR(255)` | รหัสผ่านสำหรับเข้าสู่ระบบ | `admin123` |
| `name` | `VARCHAR(255)` | ชื่อ-นามสกุล จริงของผู้ใช้งาน | `สมชาย จันทร์แก้ว` |
| `email` | `VARCHAR(255)` | ที่อยู่อีเมลของผู้ใช้ | `admin@docms.com` |
| `role` | `VARCHAR(50)` | รหัสบทบาท (`admin`, `secretary`, `executive`, `user`) | `admin` |
| `roleName` | `VARCHAR(100)` | ชื่อภาษาไทยของบทบาท | `ผู้ดูแลระบบ`, `ธุรการ`, `ผู้บริหารระดับสูง` |
| `department` | `VARCHAR(100)` | แผนกหรือฝ่ายงานที่สังกัด | `IT`, `การเงิน`, `บริหาร` |
| `position` | `VARCHAR(100)` | ตำแหน่งทางการบริหาร/งาน | `ประธานกรรมการบริหาร / CEO` |
| `phone` | `VARCHAR(50)` | เบอร์โทรศัพท์ติดต่อ | `02-555-0199` |
| `status` | `VARCHAR(50)` | สถานะบัญชี (`active` = ใช้งานได้, `suspended` = ถูกระงับ) | `active` |
| `avatar` | `LONGTEXT` | รูปภาพโปรไฟล์ (Base64 หรือ URL) | `data:image/png;base64,...` |
| `failedAttempts` | `INT` | จำนวนครั้งที่กรอกรหัสผ่านผิดติดต่อกัน | `0`, `3` |
| `lockedUntil` | `VARCHAR(50)` | เวลาที่บัญชีจะปลดล็อก (กรณีถูกล็อกจากการกรอกผิด 5 ครั้ง) | `2026-08-18T08:00:00.000Z` |
| `pinHash` | `VARCHAR(255)` | รหัสแฮชของ PIN สำหรับยืนยันตัวตน MFA (PBKDF2) | `a1b2c3...` |
| `pinSalt` | `VARCHAR(255)` | ค่า Salt ที่ใช้สร้างแฮช PIN | `f4e3d2...` |
| `createdAt` | `VARCHAR(50)` | วันเวลาที่สร้างบัญชี | `2026-01-01T00:00:00Z` |
| `lastLogin` | `VARCHAR(50)` | วันเวลาที่เข้าสู่ระบบล่าสุด | `2026-08-18T07:13:30.000Z` |

---

### 2. ตาราง `documents` - ข้อมูลเอกสารและการลงนาม
จัดเก็บข้อมูลหลักของเอกสารดิจิทัลทุกฉบับ ประวัติเวอร์ชัน ลายเซ็นที่ประทับ และข้อคิดเห็น

| ชื่อคอลัมน์ (Column) | ชนิดข้อมูล (Data Type) | คำอธิบาย (Description) | ตัวอย่างข้อมูล |
| :--- | :--- | :--- | :--- |
| `id` | `VARCHAR(50)` [PK] | รหัสเอกสาร (Primary Key) | `doc001`, `doc1771926013618` |
| `title` | `VARCHAR(255)` | หัวข้อ / ชื่อเรื่องของเอกสาร | `บันทึกข้อความขออนุมัติงบประมาณ` |
| `description` | `TEXT` | รายละเอียดเนื้อหาของเอกสารโดยย่อ | `ขออนุมัติงบประมาณพัฒนาระบบ...` |
| `category` | `VARCHAR(100)` | หมวดหมู่เอกสาร (`บันทึกข้อความ`, `สัญญา`, `รายงาน`, `คำสั่ง`) | `บันทึกข้อความ` |
| `fileName` | `VARCHAR(255)` | ชื่อไฟล์ต้นฉบับที่ผู้ใช้อัปโหลด | `budget_approval_2569.pdf` |
| `fileStorageName` | `VARCHAR(255)` | ชื่อไฟล์ที่บันทึกจริงในโฟลเดอร์ `uploads/` (UUID) | `a4b1c2-d3e4...pdf` |
| `fileSize` | `BIGINT` | ขนาดไฟล์เอกสาร (หน่วยเป็นไบต์) | `2048576` (ประมาณ 2 MB) |
| `uploadedBy` | `VARCHAR(50)` | รหัสผู้ใช้ที่เป็นผู้อัปโหลดเอกสาร | `u002` |
| `uploadedByName` | `VARCHAR(255)` | ชื่อ-นามสกุลของผู้อัปโหลด | `สุภาพร วงศ์สวัสดิ์` |
| `assignedTo` | `LONGTEXT` (JSON) | รหัสผู้ใช้งานที่ได้รับมอบหมายให้เข้าถึง/อนุมัติ/ลงนาม | `["u003", "u004"]` |
| `status` | `VARCHAR(50)` | รหัสสถานะเอกสาร (`draft`, `pending_approval`, `approved`, `signed`, `rejected`, `expired`) | `signed` |
| `statusText` | `VARCHAR(100)` | ชื่อภาษาไทยของสถานะเอกสาร | `ลงนามแล้ว`, `รออนุมัติ`, `ฉบับร่าง` |
| `priority` | `VARCHAR(50)` | ระดับความสำคัญ (`low`, `medium`, `high`, `urgent`) | `high` |
| `version` | `INT` | หมายเลขเวอร์ชันปัจจุบันของเอกสาร | `1`, `2` |
| `versionHistory` | `LONGTEXT` (JSON) | บันทึกประวัติไฟล์ทุกเวอร์ชันที่เคยอัปโหลดแก้ไข | `[{"version":1, "fileName":"..."}]` |
| `signatures` | `LONGTEXT` (JSON) | รายการประวัติการลงนาม พร้อมลายเซ็นและเวลา Server Timestamp | `[{"signedBy":"u001", "signedAt":"..."}]` |
| `comments` | `LONGTEXT` (JSON) | ข้อคิดเห็น/บันทึกแนบท้ายเอกสาร | `[{"by":"u004", "text":"อนุมัติตามเสนอ"}]` |
| `createdAt` | `VARCHAR(50)` | วันเวลาที่สร้างเอกสาร | `2026-02-15T09:00:00Z` |
| `updatedAt` | `VARCHAR(50)` | วันเวลาที่มีการแก้ไขเอกสารล่าสุด | `2026-08-18T07:13:30.000Z` |
| `deadline` | `VARCHAR(50)` | กำหนดเวลาสิ้นสุดการดำเนินงาน (Deadline) | `2026-02-28T17:00:00Z` |
| `expiryDate` | `VARCHAR(50)` | วันหมดอายุของเอกสาร (Expiry Date) | `2026-12-31T23:59:59Z` |
| `rejectReason` | `TEXT` | เหตุผลที่ปฏิเสธเอกสาร (กรณีถูก Reject) | `เอกสารแนบไม่ครบถ้วน` |

---

### 3. ตาราง `signatures` - ลายมือชื่ออิเล็กทรอนิกส์ส่วนบุคคล
จัดเก็บข้อมูลลายมือชื่อที่ผู้ใช้บันทึกไว้ในระบบเพื่อนำไปใช้ประทับลงในเอกสาร

| ชื่อคอลัมน์ (Column) | ชนิดข้อมูล (Data Type) | คำอธิบาย (Description) | ตัวอย่างข้อมูล |
| :--- | :--- | :--- | :--- |
| `id` | `VARCHAR(50)` [PK] | รหัสลายเซ็น (Primary Key) | `sig1771926013618` |
| `userId` | `VARCHAR(50)` | รหัสผู้ใช้เจ้าของลายเซ็น | `u001` |
| `signatureType` | `VARCHAR(50)` | รูปแบบลายเซ็น (`draw` = วาด, `image` = อัปโหลดรูป, `type` = พิมพ์) | `draw` |
| `signatureData` | `LONGTEXT` | ข้อมูลรูปภาพลายเซ็น (Base64 PNG Data URI) | `data:image/png;base64,iVBOR...` |
| `iv` | `VARCHAR(100)` | เวกเตอร์เริ่มต้นสำหรับการเข้ารหัส AES-256 GCM | `a1b2c3d4...` |
| `tag` | `VARCHAR(100)` | Authentication Tag สำหรับตรวจสอบความถูกต้องของการถอดรหัส | `e5f6g7h8...` |
| `createdAt` | `VARCHAR(50)` | วันเวลาที่สร้างลายเซ็น | `2026-01-10T10:00:00Z` |
| `updatedAt` | `VARCHAR(50)` | วันเวลาที่อัปเดตลายเซ็นล่าสุด | `2026-08-18T07:00:00Z` |

---

### 4. ตาราง `certificates` - ใบรับรองดิจิทัล (CA)
จัดเก็บใบรับรองดิจิทัลสำหรับยืนยันความถูกต้องและความน่าเชื่อถือของผู้ลงนามตามมาตรฐาน PKI

| ชื่อคอลัมน์ (Column) | ชนิดข้อมูล (Data Type) | คำอธิบาย (Description) | ตัวอย่างข้อมูล |
| :--- | :--- | :--- | :--- |
| `id` | `VARCHAR(50)` [PK] | รหัสใบรับรอง (Primary Key) | `cert1771926013618` |
| `userId` | `VARCHAR(50)` | รหัสผู้ใช้ที่เป็นผู้ถือครองใบรับรอง | `u004` |
| `userName` | `VARCHAR(255)` | ชื่อ-นามสกุลของผู้ถือครองใบรับรอง | `ดร.ประพันธ์ ศรีสุข` |
| `certSerial` | `VARCHAR(100)` [Unique] | หมายเลขซีเรียลของใบรับรอง (Serial Number) | `CERT-MM0UBQW7` |
| `issuedBy` | `VARCHAR(255)` | องค์กรหรือหน่วยงานผู้ออกใบรับรอง (Issuer) | `DocMS Internal CA` |
| `issuedDate` | `VARCHAR(50)` | วันที่ออกใบรับรอง (YYYY-MM-DD) | `2026-01-01` |
| `expiredDate` | `VARCHAR(50)` | วันหมดอายุของใบรับรอง (YYYY-MM-DD) | `2027-01-01` |
| `certStatus` | `VARCHAR(50)` | สถานะใบรับรอง (`active` = ใช้งานได้, `revoked` = เพิกถอน, `expired` = หมดอายุ) | `active` |
| `publicKey` | `TEXT` | กุญแจสาธารณะ (Public Key) สำหรับตรวจสอบลายเซ็น | `RSA-9A8B7C6D5E4F...` |
| `createdAt` | `VARCHAR(50)` | วันเวลาที่สร้างข้อมูลใบรับรอง | `2026-01-01T00:00:00Z` |

---

### 5. ตาราง `notifications` - ระบบการแจ้งเตือน
จัดเก็บข้อความแจ้งเตือนผู้ใช้งานแต่ละคนเมื่อมีกิจกรรมที่เกี่ยวข้องกับตนเอง

| ชื่อคอลัมน์ (Column) | ชนิดข้อมูล (Data Type) | คำอธิบาย (Description) | ตัวอย่างข้อมูล |
| :--- | :--- | :--- | :--- |
| `id` | `VARCHAR(50)` [PK] | รหัสการแจ้งเตือน (Primary Key) | `notif1771951669851` |
| `userId` | `VARCHAR(50)` | รหัสผู้ใช้ผู้รับการแจ้งเตือน | `u002` |
| `type` | `VARCHAR(50)` | ประเภทการแจ้งเตือน (`status_change`, `access_request`, `access_approved`) | `status_change` |
| `title` | `VARCHAR(255)` | หัวข้อการแจ้งเตือน | `เอกสารได้รับการลงนาม` |
| `message` | `TEXT` | เนื้อหารายละเอียดของการแจ้งเตือน | `เอกสาร "บันทึกข้อความ" ได้รับการลงนามโดย สมชาย` |
| `documentId` | `VARCHAR(50)` | รหัสเอกสารที่เกี่ยวข้อง (สำหรับกดลิงก์ไปดู) | `doc001` |
| `isRead` | `TINYINT(1)` | สถานะการเปิดอ่าน (`0` = ยังไม่อ่าน, `1` = อ่านแล้ว) | `0` หรือ `1` |
| `createdAt` | `VARCHAR(50)` | วันเวลาที่ส่งการแจ้งเตือน | `2026-02-24T16:47:49.851Z` |

---

### 6. ตาราง `access_requests` - คำร้องขอสิทธิ์เข้าถึงเอกสาร
จัดเก็บคำร้องขอเปิดสิทธิ์เข้าถึงเอกสารกรณีผู้ใช้ทั่วไปต้องการดูเอกสารที่ตนไม่มีสิทธิ์

| ชื่อคอลัมน์ (Column) | ชนิดข้อมูล (Data Type) | คำอธิบาย (Description) | ตัวอย่างข้อมูล |
| :--- | :--- | :--- | :--- |
| `id` | `VARCHAR(50)` [PK] | รหัสคำร้องขอสิทธิ์ (Primary Key) | `req1771926013618` |
| `userId` | `VARCHAR(50)` | รหัสผู้ใช้ที่ยื่นคำร้อง | `u003` |
| `userName` | `VARCHAR(255)` | ชื่อ-นามสกุลของผู้ยื่นคำร้อง | `วิทยา สมบูรณ์` |
| `documentId` | `VARCHAR(50)` | รหัสเอกสารที่ขอเข้าถึง | `doc002` |
| `documentTitle` | `VARCHAR(255)` | ชื่อเรื่องของเอกสารที่ขอเข้าถึง | `สัญญาจ้างพนักงานใหม่` |
| `reason` | `TEXT` | เหตุผลความจำเป็นในการขอเข้าถึงเอกสาร | `เพื่อใช้ประกอบการเบิกจ่ายงบประมาณ` |
| `status` | `VARCHAR(50)` | สถานะคำร้อง (`pending` = รอดำเนินการ, `approved` = อนุมัติแล้ว, `rejected` = ปฏิเสธ) | `pending` |
| `statusText` | `VARCHAR(100)` | ชื่อภาษาไทยของสถานะคำร้อง | `รอดำเนินการ`, `อนุมัติแล้ว` |
| `createdAt` | `VARCHAR(50)` | วันเวลาที่ยื่นคำร้อง | `2026-08-18T07:00:00Z` |
| `reviewedBy` | `VARCHAR(50)` | รหัสผู้ดูแลระบบที่พิจารณาคำร้อง | `u001` |
| `reviewedByName` | `VARCHAR(255)` | ชื่อ-นามสกุลของผู้พิจารณาคำร้อง | `สมชาย จันทร์แก้ว` |
| `reviewedAt` | `VARCHAR(50)` | วันเวลาที่พิจารณาคำร้อง | `2026-08-18T07:05:00Z` |

---

### 7. ตาราง `document_access` - สิทธิ์การเข้าถึงเอกสารเฉพาะบุคคล
จัดเก็บตารางควบคุมสิทธิ์ระดับละเอียด (Granular Access Control) ในแต่ละเอกสาร

| ชื่อคอลัมน์ (Column) | ชนิดข้อมูล (Data Type) | คำอธิบาย (Description) | ตัวอย่างข้อมูล |
| :--- | :--- | :--- | :--- |
| `id` | `VARCHAR(50)` [PK] | รหัสรายการสิทธิ์ (Primary Key) | `acc1771926013618` |
| `docId` | `VARCHAR(50)` | รหัสเอกสาร | `doc001` |
| `docTitle` | `VARCHAR(255)` | ชื่อเอกสาร | `บันทึกข้อความขออนุมัติงบประมาณ` |
| `roleId` | `VARCHAR(50)` | รหัสบทบาทที่ได้รับสิทธิ์ (ถ้ามี) | `executive` |
| `userId` | `VARCHAR(50)` | รหัสผู้ใช้ที่ได้รับสิทธิ์เฉพาะบุคคล (ถ้ามี) | `u003` |
| `userName` | `VARCHAR(255)` | ชื่อผู้ใช้ที่ได้รับสิทธิ์ | `วิทยา สมบูรณ์` |
| `canView` | `TINYINT(1)` | สิทธิ์ในการดูเอกสาร (`1` = มีสิทธิ์, `0` = ไม่มี) | `1` |
| `canDownload` | `TINYINT(1)` | สิทธิ์ในการดาวน์โหลดไฟล์ (`1` = มีสิทธิ์, `0` = ไม่มี) | `1` |
| `canEdit` | `TINYINT(1)` | สิทธิ์ในการแก้ไขข้อมูลเอกสาร (`1` = มีสิทธิ์, `0` = ไม่มี) | `0` |
| `canSign` | `TINYINT(1)` | สิทธิ์ในการลงนามเอกสาร (`1` = มีสิทธิ์, `0` = ไม่มี) | `1` |
| `createdAt` | `VARCHAR(50)` | วันเวลาที่ให้สิทธิ์ | `2026-08-18T07:00:00Z` |

---

### 8. ตาราง `activity_logs` - บันทึกประวัติการใช้งาน (Audit Logs)
จัดเก็บบันทึกประวัติการกระทำทั้งหมดในระบบ เพื่อความปลอดภัยและการตรวจสอบย้อนหลัง (Audit Trail)

| ชื่อคอลัมน์ (Column) | ชนิดข้อมูล (Data Type) | คำอธิบาย (Description) | ตัวอย่างข้อมูล |
| :--- | :--- | :--- | :--- |
| `id` | `VARCHAR(50)` [PK] | รหัสบันทึกกิจกรรม (Primary Key) | `log1771926013618` |
| `userId` | `VARCHAR(50)` | รหัสผู้ใช้ที่ทำกิจกรรม | `u001` |
| `userName` | `VARCHAR(255)` | ชื่อ-นามสกุลของผู้ทำกิจกรรม | `สมชาย จันทร์แก้ว` |
| `action` | `VARCHAR(100)` | รหัสกิจกรรม (`login`, `logout`, `upload`, `sign`, `approve`, `reject`, `download`) | `sign` |
| `actionText` | `VARCHAR(255)` | คำอธิบายกิจกรรมภาษาไทย | `ลงนามเอกสาร (MFA)`, `เข้าสู่ระบบ`, `สร้างเอกสารใหม่` |
| `target` | `VARCHAR(100)` | รหัสเป้าหมายที่ถูกกระทำ (เช่น รหัสเอกสาร, รหัสผู้ใช้) | `doc001` |
| `targetName` | `VARCHAR(255)` | ชื่อของเป้าหมายที่ถูกกระทำ | `บันทึกข้อความขออนุมัติงบประมาณ` |
| `timestamp` | `VARCHAR(50)` | วันเวลาที่ทำกิจกรรม (ISO 8601 Format) | `2026-08-18T07:13:30.000Z` |
| `ip` | `VARCHAR(100)` | หมายเลข IP Address ของเครื่องผู้ใช้งาน | `127.0.0.1`, `::1`, `192.168.1.100` |
